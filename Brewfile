# ── Languages ─────────────────────────────────────────────
brew "go"
brew "node"
brew "zig"
brew "nim"
brew "mise"           # language version manager (node/python/go etc.)
brew "bun"            # JS runtime + package manager
brew "deno"           # JS/TS runtime
brew "pnpm"           # fast Node package manager
brew "uv"             # fast Python package manager

# ── Shell & Terminal ──────────────────────────────────────
brew "starship"
brew "zsh-autosuggestions"
brew "zsh-syntax-highlighting"
brew "zsh-completions"

# ── Modern CLI ────────────────────────────────────────────
brew "eza"            # modern ls
brew "bat"            # syntax-highlighted cat
brew "fd"             # modern find
brew "ripgrep"        # fast grep
brew "fzf"            # fuzzy finder
brew "zoxide"         # smart cd
brew "delta"          # better git diff
brew "btop"           # system monitor
brew "jq"             # JSON processor
brew "xh"             # modern curl
brew "tldr"           # simplified man pages
brew "direnv"         # per-project env vars

# ── Git ───────────────────────────────────────────────────
brew "git"
brew "git-lfs"        # large file support
brew "gh"             # GitHub CLI
brew "lazygit"        # TUI git client
brew "gnupg"          # GPG signing
brew "pinentry-mac"   # GPG PIN entry

# ── Editor ────────────────────────────────────────────────
brew "neovim"
brew "tmux"

# ── Containers & Kubernetes ───────────────────────────────
brew "kubectl"
brew "kubectx"        # switch clusters/namespaces
brew "k9s"            # Kubernetes TUI
brew "helm"           # Kubernetes package manager
brew "stern"          # multi-pod log tailing

# ── Cloud CLIs ────────────────────────────────────────────
brew "awscli"
brew "azure-cli"      # az コマンド
brew "aztfexport"     # Azure リソース → Terraform 変換
brew "terraform"
cask "google-cloud-sdk"

# ── Networking & APIs ─────────────────────────────────────
brew "grpcurl"        # gRPC client
brew "mkcert"         # local SSL certs
brew "cloudflared"    # Cloudflare tunnel
brew "nmap"           # network scanner
brew "httpie"         # HTTP client (GUI alternative to xh)

# ── Databases ─────────────────────────────────────────────
brew "postgresql@17"  # PostgreSQL client tools
brew "redis"          # Redis client tools

# ── GUI Apps (Casks) ──────────────────────────────────────
# ── Swift / iOS ───────────────────────────────────────────
brew "xcodes"         # Xcode version manager
brew "aria2"          # xcodes の高速ダウンロードに必要
brew "swiftformat"    # Swift formatter
brew "swiftlint"      # Swift linter
brew "xcbeautify"     # xcodebuild の出力を見やすく
brew "xcodegen"       # project.yml から Xcode プロジェクト生成

# ── GUI Apps (Casks) ──────────────────────────────────────
cask "ghostty"        # terminal
cask "cursor"         # AI editor
cask "claude"         # Claude Desktop
cask "copilot-cli"    # GitHub Copilot CLI
cask "docker"         # Docker Desktop
cask "visual-studio-code"
cask "visual-studio-code@insiders"
cask "rectangle"      # window management
cask "tableplus"      # database GUI

# ── Fonts ─────────────────────────────────────────────────
cask "font-hack-nerd-font"
